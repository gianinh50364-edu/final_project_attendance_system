meta {
  name: Mark Attendance - Absent
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}{{apiPath}}/mark_attendance.php
  body: json
  auth: none
}

body:json {
  {
    "student_id": {{testStudentId}},
    "status": "absent",
    "date": "{{testDate}}"
  }
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Should mark attendance as absent", function() {
    const jsonData = res.getBody();
    expect(jsonData).to.have.property('success');
    expect(jsonData.success).to.be.true;
    expect(jsonData).to.have.property('message');
    expect(jsonData).to.have.property('action');
  });
}